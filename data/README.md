# data/ README

Place input assets here:

- `charact2.bmp` — the source image used by Steps 1–6.
- `dataset_2025.zip` — the full dataset for Step 7 (the code auto-unzips it to `data/dataset_2025/` on first run).

Notes:
- This folder is ignored by Git (see `.gitignore`). Keeping this file ensures the folder exists in the repo.
- If you decide to version small samples, remove `data/` from `.gitignore` or add finer-grained rules.
